import React from 'react';

const Options = ({freeTermsDays, upcomingDays}) => {
    return (
        <div>
            {freeTermsDays}
            {upcomingDays}
        </div>
    );
}

export default Options;
